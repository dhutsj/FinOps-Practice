apiVersion: external-secrets.io/v1beta1
kind: ExternalSecret
metadata:
  name: github-action-cost-secret
  namespace: demo
spec:
  refreshInterval: 10m
  secretStoreRef:
    name: awssecretmanager
    kind: ClusterSecretStore
  target:
    name: github-action-cost-secret
    creationPolicy: Owner
  dataFrom:
  - extract:
      key: dev/github-action/cost